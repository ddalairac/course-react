export default abstract class Service { // extends Vue {
  constructor() {
    // Service.prototype.$store = store;
    // Service.prototype.$router = router;
  }
  // protected $store: Store<iState>;
  // protected $router: VueRouter;
}
